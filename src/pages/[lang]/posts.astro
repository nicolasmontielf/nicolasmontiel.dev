---
import { isLocale } from '@/i18n/locales';

const { lang } = Astro.params;

if (!lang || !isLocale(lang)) {
    return Astro.redirect('/en');
}

return Astro.redirect('https://dev.to/nicolasmontielf', 301);
---
