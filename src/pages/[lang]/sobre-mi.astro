---
import Layout from '@/layouts/Layout.astro';
import Presentation from '@components/home/Presentation.astro';
import Certifications from '@components/home/Certifications.astro';
import { getMessages } from '@/i18n/messages';
import { DEFAULT_LOCALE, isLocale } from '@/i18n/locales';

const { lang } = Astro.params;

if (!lang || !isLocale(lang)) {
	return Astro.redirect(`/${DEFAULT_LOCALE}`);
}

if (lang === 'en') {
	return Astro.redirect('/en/about-me');
}

const messages = getMessages(lang);
---

<Layout title={messages.aboutPage.title} description={messages.aboutPage.description}>
	<section id="presentation-section">
        <Presentation variant="freelancer" greeting={messages.home.greeting} />
    </section>

    <section id="certifications">
        <Certifications
            title={messages.home.certificationsTitle}
            credentialLabel={messages.home.viewCredential}
        />
    </section>
</Layout>
